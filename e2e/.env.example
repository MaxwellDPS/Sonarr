# Seedr.cc credentials for E2E tests
# Copy this to seedr.env at the repo root (already in .gitignore)
#
# Option 1: Create seedr.env at repo root
# Option 2: Set these as environment variables

SEEDR_EMAIL=your-email@example.com
SEEDR_PASSWORD=your-seedr-password
